"use client";
import { useState } from "react";
import { AdminShell } from "@/components/admin/admin-shell";
import { PageHeader } from "@/components/common/page-header";
import { Button } from "@/components/ui/button";
import { approvalMocks } from "@/lib/mocks/admin";
export default function ApprovalPage() { const [items, setItems] = useState(approvalMocks); const [rejecting, setRejecting] = useState<string | null>(null); const [reason, setReason] = useState(""); function approve(id: string) { setItems((all) => all.filter((item) => item.id !== id)); } function reject() { if (rejecting && reason.trim()) { setItems((all) => all.filter((item) => item.id !== rejecting)); setRejecting(null); setReason(""); } } return <AdminShell active="/admin/vouchers/approval"><PageHeader title="Duyệt Voucher" subtitle="Xét duyệt các ưu đãi do Partner gửi lên."/><div className="space-y-4">{items.length ? items.map((item) => <article key={item.id} className="flex flex-wrap items-center gap-4 rounded-xl border border-slate-200 bg-white p-5 shadow-brand-sm"><div className="flex-1"><span className="text-xs font-semibold text-slate-500">{item.partner} · {item.submitted}</span><h2 className="mt-1 text-sm font-extrabold text-slate-900">{item.title}</h2></div><Button size="sm" onClick={() => approve(item.id)}>Duyệt</Button><Button size="sm" variant="danger" onClick={() => setRejecting(item.id)}>Từ chối</Button></article>) : <div className="rounded-xl border border-dashed border-slate-300 bg-white p-12 text-center text-sm text-slate-500">Không còn voucher chờ duyệt.</div>}</div>{rejecting && <div className="fixed inset-0 z-40 grid place-items-center bg-slate-950/40 p-5"><div className="w-full max-w-md rounded-xl bg-white p-6 shadow-brand-lg"><h2 className="font-extrabold">Lý do từ chối</h2><textarea value={reason} onChange={(event) => setReason(event.target.value)} className="mt-4 min-h-28 w-full rounded-lg border border-slate-200 p-3 text-sm outline-none focus:border-primary" placeholder="Nhập lý do bắt buộc..."/><div className="mt-4 flex justify-end gap-2"><Button variant="ghost" onClick={() => setRejecting(null)}>Hủy</Button><Button variant="danger" disabled={!reason.trim()} onClick={reject}>Xác nhận từ chối</Button></div></div></div>}</AdminShell>; }
