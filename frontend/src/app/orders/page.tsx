import { ProtectedPage } from "@/components/auth/protected-page";
import { CustomerShell } from "@/components/customer/customer-shell";
import { PageHeader } from "@/components/common/page-header";
import { orderMocks } from "@/lib/mocks/customer";
export default function OrdersPage() { return <ProtectedPage role="customer"><CustomerShell active="/orders"><PageHeader title="Lịch sử đơn hàng" subtitle="Theo dõi các đơn hàng và voucher đã mua."/><div className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-brand-sm"><div className="overflow-x-auto"><table className="w-full min-w-[620px] text-left text-xs"><thead className="bg-slate-50 text-[10px] uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-3">Mã đơn</th><th className="px-5 py-3">Ngày mua</th><th className="px-5 py-3">Voucher</th><th className="px-5 py-3">Tổng tiền</th><th className="px-5 py-3">Trạng thái</th></tr></thead><tbody>{orderMocks.map((order) => <tr className="border-t border-slate-100" key={order.id}><td className="px-5 py-4 font-mono font-bold text-primary">{order.id}</td><td className="px-5 py-4 text-slate-600">{order.date}</td><td className="px-5 py-4 text-slate-600">{order.items}</td><td className="px-5 py-4 font-semibold">{order.total.toLocaleString("vi-VN")}đ</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-[10px] font-bold ${order.status === "completed" ? "bg-emerald-50 text-success" : "bg-red-50 text-danger"}`}>{order.status === "completed" ? "Hoàn tất" : "Đã hoàn tiền"}</span></td></tr>)}</tbody></table></div></div></CustomerShell></ProtectedPage>; }
