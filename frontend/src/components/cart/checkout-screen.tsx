"use client";
import { useRouter } from "next/navigation";
import { useForm } from "react-hook-form";
import { z } from "zod";
import { zodResolver } from "@hookform/resolvers/zod";
import { TopNav } from "@/components/navigation/top-nav";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { State } from "@/components/common/state";
import { useCartStore } from "@/stores/cart-store";
const checkoutSchema = z.object({ name: z.string().min(2, "Nhập họ tên người nhận"), email: z.string().email("Nhập email hợp lệ"), phone: z.string().regex(/^(0|\+84)\d{9,10}$/, "Số điện thoại không hợp lệ"), payment: z.enum(["momo", "vnpay", "bank"]) });
type CheckoutValues = z.infer<typeof checkoutSchema>;
export function CheckoutScreen() { const router = useRouter(); const { items, clear } = useCartStore(); const { register, handleSubmit, formState: { errors, isSubmitting } } = useForm<CheckoutValues>({ resolver: zodResolver(checkoutSchema), defaultValues: { payment: "momo" } }); const total = items.reduce((sum, item) => sum + item.price * item.quantity, 0); async function submit(_values: CheckoutValues) { await new Promise((resolve) => setTimeout(resolve, 400)); const code = `ECO-${Math.floor(100000 + Math.random() * 900000)}`; clear(); router.push(`/order-confirmation/${code}`); } return <main className="min-h-screen bg-slate-50"><TopNav/><div className="mx-auto max-w-[1000px] px-5 py-9"><h1 className="text-2xl font-extrabold tracking-[-.5px] text-slate-900">Thanh toán</h1><p className="mt-1 text-sm text-slate-500">Hoàn tất thông tin để nhận voucher ngay.</p>{!items.length ? <div className="mt-7"><State icon="🛒" title="Chưa có voucher để thanh toán" text="Hãy thêm voucher vào giỏ hàng trước."/></div> : <form onSubmit={handleSubmit(submit)} className="mt-7 grid gap-6 lg:grid-cols-[1fr_330px]"><section className="space-y-5 rounded-xl border border-slate-200 bg-white p-6 shadow-brand-sm"><h2 className="font-extrabold text-slate-900">Thông tin nhận voucher</h2><Input label="Họ và tên" placeholder="Nguyễn Văn An" error={errors.name?.message} {...register("name")}/><Input label="Email nhận mã voucher" type="email" placeholder="ten@email.com" error={errors.email?.message} {...register("email")}/><Input label="Số điện thoại" placeholder="09x xxxx xxxx" error={errors.phone?.message} {...register("phone")}/><div><h2 className="mt-7 font-extrabold text-slate-900">Phương thức thanh toán</h2><div className="mt-3 space-y-2">{[["momo", "Ví MoMo"], ["vnpay", "VNPay"], ["bank", "Chuyển khoản ngân hàng"]].map(([value, label]) => <label key={value} className="flex cursor-pointer items-center gap-3 rounded-lg border border-slate-200 p-3 text-sm text-slate-700"><input type="radio" value={value} className="accent-[#4F46E5]" {...register("payment")}/><span className="font-semibold">{label}</span><span className="ml-auto text-xs text-slate-400">Payment mock</span></label>)}</div></div></section><aside className="h-fit rounded-xl border border-slate-200 bg-white p-5 shadow-brand-md"><h2 className="font-extrabold text-slate-900">Đơn hàng của bạn</h2><div className="mt-4 space-y-3">{items.map((item) => <div className="flex justify-between gap-4 text-xs" key={item.id}><span className="text-slate-600">{item.quantity} × {item.title}</span><b>{(item.price * item.quantity).toLocaleString("vi-VN")}đ</b></div>)}</div><div className="my-5 flex justify-between border-y border-slate-100 py-4"><b>Tổng cộng</b><b className="text-primary">{total.toLocaleString("vi-VN")}đ</b></div><Button type="submit" size="lg" fullWidth disabled={isSubmitting}>{isSubmitting ? "Đang tạo đơn..." : "Xác nhận thanh toán"}</Button><p className="mt-3 text-center text-[11px] text-slate-400">Đây là luồng thanh toán mô phỏng.</p></aside></form>}</div></main>; }
