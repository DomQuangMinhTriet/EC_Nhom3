import Link from "next/link";
import type { ReactNode } from "react";
const links = [["/partner/dashboard", "Dashboard"], ["/partner/vouchers", "Voucher của tôi"], ["/partner/vouchers/new", "Tạo voucher mới"], ["/partner/profile", "Hồ sơ Partner"], ["/partner/reports", "Báo cáo & Thống kê"], ["/partner/notifications", "Thông báo"], ["/partner/settings", "Cài đặt"], ["/partner/branches", "Nhân viên & Chi nhánh"]] as const;
export function PartnerShell({ children, active }: { children: ReactNode; active: string }) { return <main className="min-h-screen bg-slate-50 lg:flex"><aside className="border-b border-white/10 bg-[#1a1742] p-4 text-slate-300 lg:min-h-screen lg:w-60"><Link href="/partner/dashboard" className="flex items-center gap-2 font-extrabold text-white"><span className="grid h-8 w-8 place-items-center rounded-lg bg-primary text-xs">EC</span>Partner Portal</Link><nav className="mt-7 flex gap-1 overflow-x-auto lg:block">{links.map(([href, label]) => <Link href={href} key={href} className={`block shrink-0 rounded-lg px-3 py-2.5 text-xs font-semibold ${active === href ? "bg-indigo-500/20 text-indigo-200" : "hover:bg-white/5"}`}>{label}</Link>)}</nav></aside><section className="min-w-0 flex-1 p-5 md:p-8">{children}</section></main>; }
