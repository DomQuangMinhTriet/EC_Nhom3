import Link from "next/link";
import type { Voucher } from "@/lib/mocks/vouchers";
const tones = { indigo: "from-indigo-950 to-indigo-700", orange: "from-orange-900 to-orange-500", emerald: "from-emerald-950 to-emerald-600", rose: "from-rose-950 to-rose-600" };
export function VoucherCard({ voucher }: { voucher: Voucher }) { return <Link href={`/vouchers/${voucher.id}`} className="group overflow-hidden rounded-xl border border-slate-200 bg-white shadow-brand-sm transition hover:-translate-y-0.5 hover:shadow-brand-lg"><div className={`relative flex h-32 items-center justify-center bg-gradient-to-br ${tones[voucher.imageTone]} p-5 text-center text-white`}><span className="absolute left-3 top-3 rounded-full bg-white/15 px-2 py-1 text-[10px] font-bold backdrop-blur">{voucher.category}</span><strong className="text-3xl tracking-[-2px]">−{voucher.discount}%</strong><span className="absolute bottom-3 text-[11px] text-white/65">{voucher.brand}</span></div><div className="p-4"><p className="text-xs font-semibold text-slate-500">{voucher.brand}</p><h3 className="mt-1 min-h-10 text-sm font-extrabold leading-5 text-slate-900">{voucher.title}</h3><p className="mt-3 text-[11px] text-slate-500">★ {voucher.rating} · Đã bán {voucher.sold.toLocaleString("vi-VN")}</p><div className="mt-3 flex items-center justify-between"><span><b className="text-lg text-primary">{voucher.price === 0 ? "Free" : `${(voucher.price / 1000).toLocaleString("vi-VN")}K`}</b><s className="ml-2 text-[11px] text-slate-400">{(voucher.originalPrice / 1000).toLocaleString("vi-VN")}K</s></span><span className="grid h-8 w-8 place-items-center rounded-lg bg-primary text-lg font-bold text-white group-hover:bg-primary-hover">+</span></div></div></Link>; }
